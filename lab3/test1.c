#include <stdio.h>

int main(){
    //for(int(x)=num ; x<>n ; x=x+1)
    for(int(x)=0 ; x<10 ; x++){
        printf("x = %d\t",x);       //\t = tap
    }

    int i = 0;
    while(i<10){
        printf("i = %d\n",i);
        i++;
    }
    return(0);
}